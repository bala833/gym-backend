from datetime import date
from workout.models import UserProfile

def deactivate_job():
	print('ddddddddddddddddddddddddddddaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa')
    # users = UserProfile.objects.all()
    # today = date.today()
    # print("Today date is: ", today)
    # data = {"Today Data is " : today}
    # for user in users:
    #     if (user.valid_to):
    #         if (user.valid_to < today):
    #             User.objects.filter(pk=user.user.id).update(is_active=False)
    #             print(f"user are goin to expire :{user.user.username}" )

